public class Card
{
    // instance variables
    private String rank;
    private char suit;

    // The default constructor should set rank to be an empty String
    // and suit to be a space character (since a char cannot be empty).
    public Card()
    {
    }

    // This constructor should set the instance variables appropriately
    public Card( String r, char s )
    {
    }

    // make a setter and getter for each instance variable


    // The output for 2 of hearts should be "2H"
    public String toString()
    {
        return ;
    }
}
